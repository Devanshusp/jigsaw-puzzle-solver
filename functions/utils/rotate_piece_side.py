"""
rotate_piece_side.py - Utility function to rotate a piece's side.
"""


def rotate_piece_side(piece_path: str, side: str, orientation: str):
    # step 1: extract piece data
    # using center + corners data, calculate the angle of roation
    # step 2: rotate the piece
    # ensure oritentation matches, if not, rotate 180
    # return rotated contours (extract the side contours only)
    pass
